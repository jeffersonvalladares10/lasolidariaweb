/* eslint-disable react/prop-types */
/*import OrgItem from "./OrgItem";

const OrgList = ({ orgs }) => {
    return (
        <div>
        <ul role="list">
            {orgs.map((org, index) => {
            return (
                <li key={org._id} role="listitem">
                <OrgItem {...org} index={index} />
                </li>
            );
            })}
        </ul>
        </div>
    );
};

export default OrgList;*/

